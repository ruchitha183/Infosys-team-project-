Smart Air Quality - Frontend (React + Bootstrap)
-----------------------------------------------
Instructions:
1. unzip this folder
2. cd into frontend_bootstrap
3. run: npm install
4. run: npm start
5. open http://localhost:3000

This frontend uses Bootstrap via CDN and Chart.js (installed via npm).
Prediction form sends POST to http://127.0.0.1:5000/predict (Flask backend).
